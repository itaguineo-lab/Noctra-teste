# Refatoração aplicada – auditoria técnica\n\n## Melhorias priorizadas\n\n1. **Eliminar recálculo duplicado de atributos**\n   - Centralizar `recalculateStats()` no `playerService`\n   - Remover chamadas redundantes nos handlers\n\n2. **Persistência de combate**\n   - Migrar `activeFights` do `Map()` em memória para MongoDB\n\n3. **Concorrência de salvamento**\n   - Implementar merge seguro antes de `savePlayer()`\n\n## Próxima etapa recomendada\nCriar `equipmentService.js` para centralizar equipar/desequipar e reduzir bugs de inventário.\n