// Próxima integração aplicada em etapas\n// Este arquivo documenta a substituição da lógica manual por equipmentService\n\nconst { equipItem, unequipItem } = require('../core/player/equipmentService');\n\nasync function applyEquip(player, slot, item, savePlayer, userId) {\n  equipItem(player, slot, item);\n  return savePlayer(userId, player);\n}\n\nasync function applyUnequip(player, slot, savePlayer, userId) {\n  unequipItem(player, slot);\n  return savePlayer(userId, player);\n}\n\nmodule.exports = { applyEquip, applyUnequip };\n